---
description: Auto-pilot remaining GSD phases (plan, execute, audit, gap closure)
argument-hint: "[start-phase] [--dry-run]"
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, Agent, AskUserQuestion
---

Invoke the `gsdu:autopilot` skill with arguments: $ARGUMENTS

CRITICAL: After the skill loads successfully, execute the workflow steps immediately. Do NOT call the Skill tool again — the skill is already loaded and its WORKFLOW.md content is in your context.
